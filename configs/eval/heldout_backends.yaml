unsupported: true
unsupported_reason: Backend-axis splitting is implemented, but no executable data path currently produces genuine backend evidence or backend IDs.
evaluation: heldout_backends
split_strategy: axis_holdout
holdout_axis: backend_id
