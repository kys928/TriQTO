{
  "batch_size": 32,
  "checkpoint_every_epochs": 1,
  "deterministic_algorithms": true,
  "device": "auto",
  "dtype": "float32",
  "early_stopping_patience": 5,
  "gradient_accumulation_steps": 2,
  "keep_best_checkpoint": true,
  "loss": {
    "action_rank_distribution_weight": 0.5,
    "action_reward_weight": 0.25,
    "action_selection_weight": 1.0,
    "born_hellinger_weight": 1.0,
    "born_kl_weight": 1.0,
    "diagnosis_affected_qubit_weight": 0.5,
    "diagnosis_strength_weight": 0.5,
    "diagnosis_type_weight": 1.0,
    "geometry_weight": 0.0,
    "hilbert_to_born_weight": 1.0,
    "privileged_oracle_loss_weight": 1.0,
    "topology_weight": 0.0,
    "uncertainty_weighting": false
  },
  "max_candidates_per_batch": 65536,
  "max_edges_per_batch": 262144,
  "max_gates_per_batch": 262144,
  "max_gradient_norm": 1.0,
  "max_hilbert_amplitudes_per_batch": 262144,
  "max_items": 1000000,
  "max_nodes_per_batch": 65536,
  "max_outcomes_per_batch": 262144,
  "normalize_action_features": true,
  "normalize_backend_features": true,
  "normalize_topology_features": true,
  "num_workers": 0,
  "optimizer": {
    "beta1": 0.9,
    "beta2": 0.999,
    "epsilon": 1e-08,
    "learning_rate": 0.0003,
    "momentum": 0.9,
    "name": "adamw",
    "weight_decay": 0.0001
  },
  "run_name": "triqto_phase15_6_research_pilot",
  "scheduler": {
    "minimum_learning_rate_ratio": 0.1,
    "name": "warmup_cosine",
    "warmup_steps": 100
  },
  "seed": 2026,
  "stages": [
    {"epochs": 5, "name": "diagnosis_foundation", "tasks": ["diagnosis"]},
    {"epochs": 5, "name": "action_ranking_foundation", "tasks": ["action_ranking"]},
    {"epochs": 5, "name": "born_prediction_foundation", "tasks": ["born_prediction"]},
    {"epochs": 10, "name": "joint_multitask", "tasks": ["joint_multitask"]},
    {"epochs": 5, "name": "hardware_masked", "tasks": ["hardware_masked"]}
  ],
  "topology_loss_weight": 0.0
}
